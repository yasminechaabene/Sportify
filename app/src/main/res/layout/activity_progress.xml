<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/musculation"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context=".ProgressActivity">

    <ImageView
        android:id="@+id/p_header_label"
        android:layout_width="250sp"
        android:layout_height="50sp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:src="@drawable/progress" />

    <!--<Spinner
        android:id="@+id/p_level_selector"
        android:layout_width="match_parent"
        android:layout_height="40sp"
        android:layout_below="@+id/p_header_label"
        android:spinnerMode="dropdown"
        android:background="@drawable/background_grey"
        android:entries="@array/levels_array"
        >
    </Spinner>-->

    <se.emilsjolander.stickylistheaders.ExpandableStickyListHeadersListView
        android:id="@+id/workouts_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/p_header_label"
        android:layout_marginTop="10sp"></se.emilsjolander.stickylistheaders.ExpandableStickyListHeadersListView>
</RelativeLayout>
